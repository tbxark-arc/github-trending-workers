{
  "name": "github-trending-workers",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "dev": "wrangler dev",
    "pub": "wrangler deploy",
    "build": "esbuild index.js --bundle --outfile=./build/index.js --format=esm",
    "lint": "eslint --fix ."
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "eslint": ">=5.16.0",
    "eslint-config-google": "^0.14.0",
    "wrangler": "^3.0.0"
  }
}
